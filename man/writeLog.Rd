% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logging.R
\name{writeLog}
\alias{writeLog}
\title{Write the printed output of each element of a named list to a .txt file in
log/, one after another, each preceded by its list name as a heading.}
\usage{
writeLog(
  x,
  fileName = "log",
  version = NULL,
  desc = NULL,
  logDir = "log",
  append = FALSE
)
}
\arguments{
\item{x}{Named list of objects to print/log, e.g. list(int.orig =
summary(int), int.hp = halvorsenPalmquist(int)).}

\item{fileName}{Name of the log file, without extension (e.g. "aq_bdr").}

\item{version}{Version label prepended to fileName (e.g. "v1"). The file
is written as logDir/version_fileName.txt; if NULL, the version prefix
and its separator are dropped.}

\item{desc}{Description to print at the top of the log file.}

\item{logDir}{Directory to write the log file to. Created if missing.}

\item{append}{If TRUE, add to the log file when it already exists rather
than overwriting it; if it does not exist, a new file is created. If
FALSE (the default), any existing file is overwritten.}
}
\value{
x, invisibly (so this can be used inline without breaking a pipe).
}
\description{
Write the printed output of each element of a named list to a .txt file in
log/, one after another, each preceded by its list name as a heading.
}
\examples{
tmp <- tempfile()
writeLog(list(one = 1:3, summary = summary(1:10)),
         fileName = "example", logDir = tmp)
readLines(file.path(tmp, "example.txt"))
}
